export const metadata = {
  title: "AILP Member Login | All India Labour Party",
  description: "Sign in to your All India Labour Party member portal.",
};

export default function MemberLoginLayout({ children }) {
  return children;
}
